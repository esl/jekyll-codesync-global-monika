# frozen_string_literal: true

source "https://rubygems.org"

# GitHub Pages uses Ruby 3.3.4, but allow newer for local development  
ruby ">= 3.3.0"

# Use GitHub Pages gem for guaranteed compatibility with jekyll-build-pages action
gem "github-pages", "~> 232", group: :jekyll_plugins

# Additional plugins for our site
group :jekyll_plugins do
  gem "jekyll-commonmark"
end
